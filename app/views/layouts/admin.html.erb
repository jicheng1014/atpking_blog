<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>管理后台 - <%= Setting.site_name.presence || 'My Blog' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-white shadow-sm">
        <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="flex items-center flex-shrink-0">
                <%= link_to "管理后台", admin_root_path, class: "text-xl font-bold text-gray-800" %>
              </div>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                <%= link_to "文章管理", admin_posts_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900 border-b-2 border-transparent hover:border-gray-300" %>
                <%= link_to "网站设置", admin_settings_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900 border-b-2 border-transparent hover:border-gray-300" %>
                <%= link_to "任务队列", admin_mission_control_jobs_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900 border-b-2 border-transparent hover:border-gray-300" %>
              </div>
            </div>
            <div class="flex items-center">
              <%= link_to "返回首页", root_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50" %>
            </div>
          </div>
        </div>
      </nav>

      <% if notice %>
        <div class="px-4 mx-auto mt-4 max-w-7xl sm:px-6 lg:px-8">
          <div class="p-4 text-green-700 rounded-md bg-green-50">
            <%= notice %>
          </div>
        </div>
      <% end %>

      <% if alert %>
        <div class="px-4 mx-auto mt-4 max-w-7xl sm:px-6 lg:px-8">
          <div class="p-4 text-red-700 rounded-md bg-red-50">
            <%= alert %>
          </div>
        </div>
      <% end %>

      <main class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8" >
        <%= yield %>
      </main>
    </div>
  </body>
</html>